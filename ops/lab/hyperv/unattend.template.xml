<?xml version="1.0" encoding="utf-8"?>
<!--
  KCSP lab answer file.

  The image is applied to the VHDX with DISM rather than run through Windows
  Setup, so the windowsPE pass never executes and no disk configuration belongs
  here. Windows picks this file up from \Windows\Panther on first boot and runs
  the specialize and oobeSystem passes unattended.

  Placeholders are replaced by New-KCSPWindowsBase.ps1:
    {{COMPUTERNAME}} {{TIMEZONE}} {{LOCALE}} {{ADMINUSER}} {{ADMINPASSWORD}}
-->
<unattend xmlns="urn:schemas-microsoft-com:unattend">
  <settings pass="specialize">
    <component name="Microsoft-Windows-Shell-Setup" processorArchitecture="amd64"
               publicKeyToken="31bf3856ad364e35" language="neutral" versionScope="nonSxS"
               xmlns:wcm="http://schemas.microsoft.com/WMIConfig/2002/State">
      <ComputerName>{{COMPUTERNAME}}</ComputerName>
      <TimeZone>{{TIMEZONE}}</TimeZone>
    </component>
    <component name="Microsoft-Windows-TerminalServices-LocalSessionManager" processorArchitecture="amd64"
               publicKeyToken="31bf3856ad364e35" language="neutral" versionScope="nonSxS"
               xmlns:wcm="http://schemas.microsoft.com/WMIConfig/2002/State">
      <!-- Remote Desktop stays off: the lab drives guests over PowerShell Direct. -->
      <fDenyTSConnections>true</fDenyTSConnections>
    </component>
    <component name="Microsoft-Windows-Deployment" processorArchitecture="amd64"
               publicKeyToken="31bf3856ad364e35" language="neutral" versionScope="nonSxS"
               xmlns:wcm="http://schemas.microsoft.com/WMIConfig/2002/State">
      <RunSynchronous>
        <!-- Allow local scripts so first-logon provisioning can run. -->
        <RunSynchronousCommand wcm:action="add">
          <Order>1</Order>
          <Path>powershell.exe -NoProfile -ExecutionPolicy Bypass -Command "Set-ExecutionPolicy -Scope LocalMachine RemoteSigned -Force"</Path>
          <Description>Enable local script execution</Description>
        </RunSynchronousCommand>
      </RunSynchronous>
    </component>
  </settings>

  <settings pass="oobeSystem">
    <component name="Microsoft-Windows-International-Core" processorArchitecture="amd64"
               publicKeyToken="31bf3856ad364e35" language="neutral" versionScope="nonSxS"
               xmlns:wcm="http://schemas.microsoft.com/WMIConfig/2002/State">
      <InputLocale>{{LOCALE}}</InputLocale>
      <SystemLocale>{{LOCALE}}</SystemLocale>
      <UILanguage>{{LOCALE}}</UILanguage>
      <UserLocale>{{LOCALE}}</UserLocale>
    </component>
    <component name="Microsoft-Windows-Shell-Setup" processorArchitecture="amd64"
               publicKeyToken="31bf3856ad364e35" language="neutral" versionScope="nonSxS"
               xmlns:wcm="http://schemas.microsoft.com/WMIConfig/2002/State">
      <OOBE>
        <HideEULAPage>true</HideEULAPage>
        <HideLocalAccountScreen>true</HideLocalAccountScreen>
        <HideOEMRegistrationScreen>true</HideOEMRegistrationScreen>
        <HideOnlineAccountScreens>true</HideOnlineAccountScreens>
        <HideWirelessSetupInOOBE>true</HideWirelessSetupInOOBE>
        <ProtectYourPC>3</ProtectYourPC>
        <SkipMachineOOBE>true</SkipMachineOOBE>
        <SkipUserOOBE>true</SkipUserOOBE>
      </OOBE>
      <UserAccounts>
        <LocalAccounts>
          <LocalAccount wcm:action="add">
            <Name>{{ADMINUSER}}</Name>
            <DisplayName>{{ADMINUSER}}</DisplayName>
            <Group>Administrators</Group>
            <Password>
              <Value>{{ADMINPASSWORD}}</Value>
              <PlainText>true</PlainText>
            </Password>
          </LocalAccount>
        </LocalAccounts>
      </UserAccounts>
      <!-- No AutoLogon: PowerShell Direct authenticates over the VMBus and
           works at the login screen, so the guest never needs a session. -->
      <FirstLogonCommands>
        <SynchronousCommand wcm:action="add">
          <Order>1</Order>
          <CommandLine>powershell.exe -NoProfile -ExecutionPolicy Bypass -Command "New-Item -ItemType Directory -Path C:\KCSP -Force | Out-Null; Set-Content -Path C:\KCSP\provisioned.txt -Value 'ok'"</CommandLine>
          <Description>Mark provisioning complete</Description>
        </SynchronousCommand>
      </FirstLogonCommands>
    </component>
  </settings>
</unattend>
